pipeline 
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t dual-ci-cd-app -f docker/Dockerfile .'
            }
        }

        stage('Run Container') {
            steps {
                sh '''
                    docker run -d -p 8000:8000 --name aws-ci-container dual-ci-cd-app
                    sleep 5
                '''
            }
        }

        stage('Validate Application') {
            steps {
                sh 'curl -f http://localhost:8000'
            }
        }
    }

    post {
        always {
            sh '''
                docker stop aws-ci-container || true
                docker rm aws-ci-container || true
            '''
        }
    }
}
